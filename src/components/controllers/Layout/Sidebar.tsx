import { Container } from '@/components/views/styledContainers';

export default function Sidebar() {
  return (
    <Container mobileWidth="100%" flex={1} backgroundGradient>
      SIDEBAR
    </Container>
  );
}
